# Interim verification report

Project is in research, not complete and not publicly verified.

Supported analysis build: PC downloaded resources 144 / build 51. Android 2.5.1 is used for loader/symbol analysis, not silently substituted for PC mechanics.

Recovered code covers the offensive utility, positive-STR effectiveness, separate crit/target factors, category entry points and later hit-resolution boundaries. Data identifies Strike 4427, ordinary Vulnerable 2934 and Psychic Trauma variants 80331/80335. Gameplay has not yet independently verified a complete supported path.

| Fixture | Observed | Predicted | Difference | Status |
|---|---:|---:|---:|---|
| replay-001, 珈伦 Strike level 3 | 318 | — | — | Incomplete pre-hit state |
| Copied-original offensive utility, 2,262 synthetic inputs | Runtime oracle | Exact match | 0 | Utility translation only |
| Copied-original target/crit methods, 2,097 synthetic inputs | Runtime oracle | Exact match | 0 | No card/skill-tag adapter scope; before HP resolution |
| Copied-original Passive effect and rounding, 1,052 synthetic inputs | Runtime oracle | Exact match | 0 | Explicit target/dimension getters; intercepted BeHit, no HP resolution |
| Copied-original offensive preparation, 549 synthetic inputs | Runtime oracle | Exact match | 0 | PvE Awakener, no card instance, explicit resolved properties and skill tags |

Real observation fixtures: 2 incomplete (one Normal hit and one Frenzy sequence); completed: 0. Independent holdout fixtures: 0. Largest unexplained gameplay discrepancy: not measurable yet because neither fixture can be predicted from fully established inputs. Do not report this as zero error.

Test command: `node --test tests/oracle.test.mjs tests/target-oracle.test.mjs tests/research-api.test.mjs tests/old-embers.test.mjs tests/passive-oracle.test.mjs` from `morimens-research`. All five test files passed on 2026-09-19, covering 5,411 exact synthetic original-runtime comparisons (2,262 utility, 2,097 target/crit, 1,052 Passive), input rejection cases and five Old Embers diagnostic test groups. The Old Embers cases are code-derived expectations, not runtime-oracle or gameplay comparisons. No rounding tolerance is used. The target oracle executes both original target methods; it supplies synthetic objects with no card or skill tags and excludes target slots 4/5 and HP resolution. The Passive oracle executes original __DoMultiEffect, CalFinalVal and GetDamageSubType, with an explicit dimension getter and intercepted BeHit; it does not test Fate Cut state accumulation, threshold execution or final HP loss.

Supported calculation at this checkpoint: explicitly resolved offensive utility inputs, with an arithmetic trace. Experimental mode also translates the Active crit/target formula before hit resolution, now supported by original-runtime comparisons within the adapter scope above. Card and skill-tag branches still have only code evidence; no complete gameplay validation has passed. Strict mode returns no verified final damage. No named character's complete HP-damage calculation is yet supported as verified. Unsupported public coverage includes build/equipment resolution, full defense/hit resolution, status generation/triggering, category end-to-end calculations and timing exceptions.

Publication gate: **NOT PASSED**. Website: not built or published. Next gate work is a complete ordinary attack trace followed by independent gameplay holdouts. See TEST_PLAN.md and UNCERTAINTIES.md.

Latest extension (2026-09-19): add `tests/offensive-setup.test.mjs` to the command above. All six files pass; the total is now **5,960** exact synthetic original-runtime cases (5,411 + 549). `engine/offensive-setup.mjs` prepares resolved inputs for a limited no-card PvE path, including tagged factors and positive STR effectiveness. It excludes actual card-instance branches, state/property restoration, target and HP resolution; it must not substitute for a played hand card. Its finalDamage remains null. The prior five-file checkpoint above is historical.

The shareable `output/pdf/arachne-mortal-blast-five-repetitions.pdf` uses `tools/mortal-blast-example.mjs` and the existing arithmetic pipeline for an explicitly assumed sequence. Its 57,478 and 29,139 totals are conditional examples, not gameplay observations or additional runtime-oracle comparisons. No publication coverage was added by that example.

Updated user priority: complete that trace in higher-difficulty content where practical. Frenzy replay-002 is selected; its boss-specific stack consumption and extra HP-change rule are documented in HIGH_DIFFICULTY.md. No individual high-difficulty hit has passed yet.

Additional isolated execution: `tools/state_property_routing_oracle.py` passes 288 assertions against original `BattleStateServer.ChangeOwnerProperty`. Actual property classifications determine whether a Player-owned PvE state mutates each Awakener or its owner. These routing assertions are reported separately from the 5,960 JavaScript differential vectors. They resolve how the shared flat Strike buff reaches Arachne, but do not validate replay restoration, callbacks or a complete hit. See STATE_PROPERTY_ROUTING.md.

Latest card extension: `engine/card-offensive-setup.mjs` matches 330 additional original `__GetShowDamage` + utility cases. Total differential vectors: **6,290** (5,960 + 330), with the 288 routing assertions still separate. All seven test files pass. The new adapter covers explicit card properties, instruction-card flags, non-Awake tags and state-trigger-add suppression for the ordinary PvE subtype. Actual ownership, skill-level arguments, state-derived properties, crit/target card branches and HP remain unresolved. It returns finalDamage null. Run `node --test tests/oracle.test.mjs tests/target-oracle.test.mjs tests/research-api.test.mjs tests/old-embers.test.mjs tests/passive-oracle.test.mjs tests/offensive-setup.test.mjs tests/card-setup.test.mjs`.

The PDF scenario is undergoing a user-requested revision to level 90, Soulforge 10, rouse before Exalt, 0 STR, 100% crit rate and 150% stated crit damage. Earlier example totals are superseded. Required unresolved choices are saved in `research/evidence/mortal-blast-user-scenario.json`; do not treat the intermediate PDF or baseline script as the completed answer to that revised request.

Card/crit extension: `tests/card-target.test.mjs` passes 282 additional original-runtime comparisons for Strike-tagged cards. Total differential cases now **6,572** (6,290 + 282); routing assertions remain 288 separately. The original target methods execute with synthetic actor/card/target properties, instruction and state-trigger flags, and controlled block/barrier conditions. Tests cover additive crit sources, crit scaling, noncrit suppression, conditional target slot 5, and card block/barrier application. Slot 4 is deliberately ineligible in this Strike-only scope. Barrier list resolution, actor conditional helper implementations, exclusive formula subtypes, other tags and final HP loss are not validated by this extension. The previous seven-file suite passed; the new eighth file also passes. No gameplay fixture or holdout count changed.

Hit-helper extension: `tests/hit-limits.test.mjs` passes 282 exact comparisons against original CalcBlockedDamage, ApplyPreventBeActiveDamageRetainHp and ApplyIncomingDamageLimitsBeforeHpLoss. Current total is **6,854 differential cases**, plus 288 separate routing assertions. Scope: explicit properties, living target, controlled prevent eligibility, and numerical helpers only; no immunity determination, HP mutation, property callbacks, budget-statistic updates or damage/death event execution. See DEFENSE.md. This ninth test file passes; prior component test outcomes remain unchanged. Gameplay and publication gates remain unpassed.

API composition: calculateDamage now accepts either a resolved offense vector or explicit card setup, and optionally connects the target result to shield/limit helpers using explicit immunity/prevention decisions. `tests/research-api.test.mjs` and the new `tests/active-pipeline.test.mjs` pass. These integration checks do not increase the original-runtime count. Experimental output exposes incoming damage and an HP-loss request separately; finalDamage remains null in all modes. HP mutation/callbacks, multi-hit state progression and independent gameplay are not claimed. See RESEARCH_API.md.

Argument normalization correction: 24 exact original GetSkillArgs comparisons pass in `tests/skill-arguments.test.mjs`, bringing the current component differential total to **6,878**, with routing assertions still separate. Ordinary numeric arguments ceil before explicit overrides and are stored at effect-list generation. The superseded Mouchette baseline had skipped this stage; its script now normalizes arguments and requires an explicit historical-baseline flag. See SKILL_ARGUMENT_SNAPSHOTS.md. Earlier PDFs are not valid answers to the revised user setup. No gameplay or publication gate has passed.
# Skill-argument integration follow-up

Realm correction progress: traced history-copy metadata (no original card-state serialization) and Beacon before/after-use configuration. Added explicit Prism/Beacon combo recalculation with per-card schedules and no double-counting; source-derived boundary tests pass. These are not additional original-runtime or gameplay fixtures. Final team mastery/Pure Ultra eligibility and initial turn/card state remain required to claim the complete equipped comparison.

IMPORTANT subsequent scope correction: Arachne's intrinsic realm supplies Prism; the 168,039 equipped example zeroed this as an extra. Its numerical vectors pass arithmetic checks but are not a complete ordinary Arachne-team prediction. See ARACHNE_SIGNATURE_COMPARISON.md. Eternal Weave's max main stat is +72 Realm Mastery; its +40 temporary team amplification requires Arachne-owned pursuits, not Mouchette's. Added code-derived eligibility/cap tests. Complete realm/mastery and card-copy state reconstruction remains pending; no revised full-team total is claimed.

Latest user-approved additions: Mouchette's maxed signature Wheel and 150% team Damage Amplification. Pinned SKeyDB identifies Doomsday Rampage (wheel-0029), max effect 60% Exalt/pursuit base and 25% ATK flat per played Strike. PC State 123521/Cmd124065/State124066 and counter123518 establish property placement and eight-stack cap. New conditional direct total 168,039: Blast70,856, pursuits87,933, Arachne9,250. Fourteen resolved per-hit arithmetic checks match original Lua. `output/pdf/mouchette-signature-wheel-calculation.pdf` has two rendered and visually inspected pages and supersedes the prior scenario total for this updated setup. Crit100/150 remains specified final stats. Old Embers 672,156 is a separate conditional comparison, not confirmed for the friend's run. No independent gameplay validation or publishing-gate change.

Equipped-combo gap audit: friend's message identifies missing Damage Amplification and Wheel effects but gives no numerical inputs. Added an explicit resolved-modifier overlay, placement tests, and MOUCHETTE_EQUIPPED_GAP.md. Recovered RelicConfig 84121 as a source of Arachne's seasonal follow-up marker; Rouse is not the direct gate. No equipped total is inferred, no gameplay fixture count is increased, and the publication gate remains unmet. Work remains offline while the user uses the computer.

Post-report goal progress: executed original effect scheduling on two synthetic trees, confirming child-before-repetition, root attachment behind prior siblings, and early battle termination. Added a matching synchronous scheduling subset and a separate HP/event/listener composition check. Differential fixture total becomes 6,906; the composition check and report-specific arithmetic rechecks are not counted as new gameplay evidence. Full conditions, yielding, retargeting, state commands and independent gameplay validation remain outstanding.

Approved Mouchette example completed: user explicitly accepted max skills/Gnostic, E2 Mouchette, Lv90 unroused Arachne, 100% crit/150% Crit DMG on both, five Strike/Blast pairs and an unmodified Astral Reign Dominator, retaining Mouchette SF10/Rouse-before-Exalt/0 STR. New report `output/pdf/mouchette-arachne-approved-calculation.pdf` has four pages; all four rendered pages visually inspected. Result is 97,228 conditional pre-hit damage (Mouchette 88,688; Arachne 8,540), excluding opening Exalt. All 14 resolved per-hit records match original Lua utility/target arithmetic via `tools/verify_mouchette_baseline.py`. JSON includes traces and source hashes. These report checks are not new independent gameplay fixtures or holdouts; no publication-gate change. Prior example PDFs and the disabled old builder remain superseded.

Listener-dispatch follow-up: recovered BattleEventMgr and BattleStateTriggerServer, and verified original foundation table.clone is shallow. Five original-runtime scenarios (two dispatches each) now match the JavaScript listener-ordering subset, including registration/removal during dispatch. Differential fixture total is 6,904; routing assertions remain separate. State eligibility and queued command creation are documented as code traces only in EVENT_DISPATCH.md. This does not establish complete boss phase timing or add gameplay holdouts.

HP-event follow-up: five original owner-callback cases establish event order and shared mutable payload identity; matching JavaScript enqueue helper passes all five. Source trace shows reference retention through event configuration and later dispatch. These are synthetic callback comparisons, not scheduler or listener validation. Differential total is 6,899 with 288 separate routing assertions. No independent gameplay result has been added.

Subsequent HP-property progress: added an original-runtime oracle for ordinary HP subtraction and a matching isolated JavaScript helper. All 16 boundary comparisons pass, including fractional loss, overkill, callback order and zero-delta callback invocation. Owner/sender callbacks are non-mutating spies; this does not validate event dispatch or full BeHit. Differential total is now 6,894, plus 288 separate property-routing assertions. Evidence registry rebuilt with 21 entries and source hashes verified. Independent gameplay predictions and holdouts remain incomplete; the helper does not relax the publication gate.

Connected the recovered numeric argument normalization to the research API as an explicit optional stored-ArgN input. The API rejects simultaneous resolved values and snapshot inputs, preserves fractional/zero card overrides, and records the normalization evidence. Three affected test files pass: active-pipeline, research-api and skill-arguments. This is synthetic integration coverage, not an independent gameplay prediction. No new gameplay fixture or holdout passed; publication remains gated.

Disabled the superseded Mortal Blast PDF builder before document generation. The existing PDF and intermediate JSON remain historical, incorrect for the revised user scenario; no replacement PDF or current-scenario total has been produced. The previous goal turn only restated status (no progress); this follow-up changes the calculator and verifies the affected path.

Realm extension: 19 additional original FuncTable expression cases pass for Arachne Prism/temporary Beacon layers. Prior recorded runtime total was 6,906; this adds 19 isolated expression vectors (6,925 recorded), not full action or gameplay coverage. Three affected tests pass: singularity-realm, singularity-layers-runtime, arachne-wheel. Evidence registry now has 26 entries with checked original source hashes. Pure Ultra mastery property resolution is code-derived; initial team/player mastery and Beacon timeline remain unresolved. Equipped PDF generation remains disabled pending complete team reconstruction. No publication gate changed.

Beacon extension: 24 original FuncTable eligibility cases pass with actual Rouse, Exalt, Arachne Strike and Mortal Blast type lists. This increases recorded isolated runtime vectors from 6,925 to 6,949. Counter-transition composition and pursuit trigger suppression are code-derived, not counted as runtime vectors. Three affected realm tests pass; registry has 28 entries with checked source hashes. A fresh ordinary-round Beacon schedule is now explicit; initial PlayerRole mastery remains unresolved. No independent gameplay prediction or holdout was added, and publication remains gated.

Local website progress: explicit mastery/amp/Shuttle controls now call the existing realm and damage modules and display per-hit traces. Syntax, static asset allowlist and synthetic integration checks pass. Browser/UI and WebMCP validation were not performed under the user's no-computer-control constraint. Nothing registered or deployed. Initial mastery is a required input, following the recovered PVE copyProperties initialization boundary. Registry now has 29 source-hash-checked entries; original-runtime vector counts unchanged.

Connected HP extension: 18 original BeHit/HP records match after signed-zero normalization, bringing recorded isolated runtime vectors to 6,967. Original immunity, prevention, shield, cap and property-mutation code runs; owner/sender callbacks are spies and damage-event/animation/record hooks are disabled. No full event/death or gameplay validation. Phase-cap simulator now uses this HP step; its existing code-derived transition checks pass. Registry has 31 checked entries, including the previously missing PhaseCapCommands evidence ID.

Fatal-damage extension: 8 original BERoleDeadlyDamage.DoEffect control-flow cases pass using synchronous event/role adapters. Recorded runtime vectors now 6,975. The source-derived immediate-child scheduler mode passes a healing-descendant integration check; ordinary/attached scheduling and phase-cap regression tests remain green. Actual revival states, RoleDie and gameplay validation remain incomplete. Registry has 32 source-hash-checked entries. No publication gate passed.

Reproducible checkpoint added: tools/verify_research.py runs all 23 current JavaScript test files, audits the two real-observation records and verifies all three website engine copies. Suite passed; zero observations and zero holdouts qualify for review. PublicationStatus NOT_READY, publicationAuthorized false. Four audit regression tests reject missing/nonfinite predictions, assumptions, incomplete state, missing freeze evidence and unexplained differences. Existing Frenzy screenshots were re-inspected offline and remain insufficient for an isolated prediction. No gameplay count changed.

HP-attribute extension: 10 original BEChangeAttr HP-loss cases pass through actual property mutation, bringing recorded isolated runtime vectors to 6,985. Event/death hooks remain observers. Old Embers HP-stage composition now distinguishes requested loss from actual loss and avoids double rounding fractional limits; later stack consumption remains a plan. Full suite passes across24 test files; registry has33 checked entries. Zero reconstructed gameplay predictions and holdouts; publication NOT_READY.

Old Embers live command iterator added from Cmd80572/81060 and effect condition/parameter timing. Three affected test files pass. Composition covers state changes between HP loss and stack consumption; target/death gating and full command execution remain external. No new runtime-oracle or gameplay fixtures were counted. Registry has34 checked entries.

Main API integration: experimental Active calculations with explicit hitResolution now include an ordinary HP-subtraction model after the incoming-hit request. Modeled HP loss, request, incoming amount and overflow diagnostic remain distinct. Integration cases cover caps/retention, immunity preserving shield, overkill, fractional HP, death resistance and immutable caller inputs. Strict mode still returns no experimental models and finalDamage null. Full suite passes across25 test files. No new runtime-oracle or gameplay fixtures; publication NOT_READY.

Passive main-API extension: distinct explicit Passive inputs now use the 1,052-case tested Passive arithmetic path with a rounding trace, followed optionally by common hit limits and modeled HP subtraction. Seven new original Passive BeHit cases pass; recorded runtime vectors total6,992. Active-prevention eligibility is rejected for Passive, Active crit/setup inputs cannot leak into the branch, and dead targets skip. Full suite passes across26 test files; registry35 entries. Fate Cut state execution and independent gameplay validation remain incomplete; publication NOT_READY.

Stored-main-target replay extension: the replay candidate API now accepts an explicit hit index for multi-hit windows and reconstructs `BESetTempMainTarget` from the first complete played-skill hit boundary. It associates `TempMainTarget` and `AllEnemyWithoutMainTarget` rows by the stored selection rather than later HP or hit order, and rejects incomplete identity, a prior unrelated hit, unsupported setup, ambiguous rows, and any active competing passive-damage path. Mosk command 131035 is the catalog case. The source-hashed audit now reports 121/121 linked ordinary-Active commands and 149/149 rows statically shape-compatible. Full verification passes across 132 test files; gameplay observations remain 2, reviewable predictions 0, holdouts 0, and publication NOT_READY. Server maintenance prevented a real replay download, so this closes a static adapter gap only.
