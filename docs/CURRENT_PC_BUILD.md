# Current PC combat-build boundary

The installed Steam client now uses downloaded resource 151 / content build 51. Most general calculator and theorycraft operations remain pinned to resource 150 / build 51, while the broad original-runtime suite remains pinned to resource 144 / build 51. The agent CLI accepts resource 151 for complete live-property snapshot damage, repeated snapshot damage, catalog-prepared snapshot Active damage, preparation-only skill inspection, bounded ultimate-energy calculation/effect/storage, catalog-prepared Defend Block followed by caster energy, one catalog-prepared state-to-Active handoff plus its bounded ordered and paid chains, and the recovered Wheel event/damage/payment timeline. It loads the actual resource-151 Skill, BattleApi, Cmd and State exports for catalog-backed paths; every operation outside an explicit dependency boundary fails closed. `research/evidence/pc-res144-to-res151-combat-build.json` records the installed manifest and hash-only TextAsset inventory. `research/evidence/pc-res150-to-res151-combat-carryforward.json` confirms that all 15 tracked combat modules are byte-identical between resources 150 and 151. A private full-table comparison also finds Skill, Cmd, State, BattleApi and Constant Lua-semantically equivalent: there are no added, removed or gameplay-value changes. Resource 151 serializes one empty Lua table differently and serializes contiguous command lists as JSON arrays; the importer preserves their Lua `ipairs` order. The public hash-only result is `research/evidence/pc-res150-to-res151-config-catalogs.json`.

Ultimate-energy support has its own complete boundary rather than inheriting from the replay adapter. `research/evidence/pc-res151-ulti-energy-compatibility.json` binds all seven exercised modules and confirms byte equality with the resource-150 modules that passed 516 exact inherited fixtures: 144 calculation, 120 effect-loop and 252 capped-storage cases. The API still requires explicit properties, card/type/tag decisions, target order and Awakener eligibility; callbacks, full card/command lifecycle, automatic build assembly, gameplay and holdout credit remain excluded.

Prepared Defend support additionally executes 256 `GetRealBlock` and 160 Block-storage fixtures directly on resource-151 bytecode with zero mismatches, then composes them with the unchanged Skill/Cmd catalogs and the 516-case energy boundary. `research/evidence/pc-res151-prepared-block-compatibility.json` records that composition. Automatic target generation, legality/payment, repetition, callbacks, Block events and later lifecycle remain excluded.

Prepared state-to-Active support executes 432 state/property mutation fixtures directly on resource-151 `BattleStateServer` and `BattlePropertyServer` dependencies with zero mismatches. It then composes that boundary with the five unchanged catalogs and the narrow resource-151 Active adapter. Only `run-prepared-state-active-sequence` receives this internal execution profile. `run-prepared-state-active-chain` composes that singular handoff with schema-1/4 prepared Active actions; the general role-state API and other state operations remain closed. `research/evidence/pc-res151-state-active-sequence-compatibility.json` records the dependency hashes and composition. The paid chain adds seven resource-151 modules byte-identical to the resource-150 legality/payment boundary, carrying 90 ordinary PvE play-check and 222 payment fixtures; `research/evidence/pc-res151-paid-state-active-chain-compatibility.json` records this composition. The four recovered Wheel transitions also carry forward: the relevant State/Cmd rows have zero resource-150→151 semantic changes, Doomsday continues to read live `AtkForce`, and the Active/payment boundaries support nine explicit event, damage, comparison and bounded-search operations. See `research/evidence/pc-res151-wheel-timeline-compatibility.json`.

The narrow live-property replay adapter is explicitly supported for resource 151 by exact dependency carry-forward. Its complete bounded dependency list contains six byte-identical client modules and the five semantically equivalent tables above. That adapter supports its four listed CLI operations; the ultimate-energy, Defend, singular state-to-Active, ordered-chain, paid-chain and Wheel-timeline operations each have their own additional evidence boundary. None widens Wheel, state chains, card-manager, lifecycle or other simulator operations. The composition report is `research/evidence/pc-res151-replay-adapter-compatibility.json`. A resource-151 prediction freeze must include both the installed-build identity report and this adapter compatibility report.

The paid Wheel timeline and its catalog-preparation bridge now support the
resource-150 baseline in addition to resource 144. An exact row audit covers
Doomsday Rampage, Light of Intellect, Eternal Weave, Rota Fortunae, their
counters and their four trigger commands. The Arachne and Light gameplay fields
are unchanged apart from `BaseSortID`. Resource 150 changes Doomsday's source
expression from `StateOwner.atk` to `StateOwner.AtkForce`; the API exposes the
selected source property and requires its live resolved value. The bridge was
also exercised through `tools/run_theorycraft_request.mjs` with resource-150
Skill/BattleApi/Cmd/State context. This does not carry the general Wheel or
catalog-preparation APIs forward to resource 151.

Of the selected combat modules, BattleConst, BattleUtilServer, BattleCmdServer, BattlePropertyServer, BEActiveDamage, BattleRecord and BattleEngine changed. BattleUnitBase, BattleStateServer, BEPureDamage, BEFixedDamage, BETentacleAttack, BattleReplayPlayer, PVEGameplay and BattleUnitUtil remained byte-identical. Path IDs are ignored because bundle repacking changes them without changing content.

The actual resource-150 chunks were loaded through the copied client XLua runtime. `tools/verify_current_show_damage_formula.py` evaluated all 2,262 existing ShowDamageFormula fixture vectors and matched resource 144 exactly. The changed BattleCmdServer also matches 879 inherited PvE offensive-input assembly cases: 549 without a card and 330 with an explicit non-Awake card. `tools/verify_current_target_damage.py` checks another 2,097 no-card `__GetFinalDamage` and `GetTargetBeDmgPerMul` cases. The card extension matches 506 cases covering none/Strike/Ulti/combined tags, card and tag critical contributions, Ulti and instruction-card target amplification, state-trigger exclusion and supplied block/barrier eligibility. The source hashes and results are in the corresponding `pc-res150-*-runtime.json` evidence files.

Character progression is also current-build aware. Selected installed config modules are exported to ignored private storage, while the public resource-150 payload contains only derived progression rows and source hashes. The original current-client `GetAwakerBaseAttrValue` path matches 2,700 checks across 60 resolved characters, five levels, three Gnostic ranks and CON/ATK/DEF. Supported advancement primary-stat rows are tested separately through the client rounding rule. One identity and all passive/equipment/battle-state effects remain outside this boundary.

The selected changed property paths also remain equal: 24 numeric constructor/mastery-initialization cases and 16 ordinary HP-subtraction cases match exactly. The current ultimate-energy path matches all 516 inherited cases across command calculation, effect repetition/source assembly and capped Awakener property storage. `BEGainUltiEnergy`, `BattleEffectServer` and `BattleUnitAwaker` are byte-identical; the changed command, utility and property modules reproduce the inherited outputs exactly within these fixtures. The ordinary Camp 1 PvE Defend-card Block calculation also matches all 256 inherited connected `GetRealBlock` cases, covering player/caster/card/recipient property ownership, tag factors and ParaPlus before storage. The combined BeHit path matches another 18 cases covering explicit immunity, block, puncture, retained-HP, incoming-limit and death-resistance inputs through HP mutation. The changed BattleEngine reproduces all 24 inherited ordinary PvE command-entry gate cases through the missing-card dispatch boundary. Its `CreateEventEffect` request boundary also matches all 8 inherited cases, including payload identity and defaulted or explicitly preserved `isAutoOp`. Four connected cases now continue from that request through effect-manager construction, `BESendEvent`, listener callbacks and effect completion; payload identity, auto-operation flags and ascending/head priority order match exactly. Five more cases continue `RoleHpChanged` through the actual HP listener and generic state-trigger eligibility, preserving monster-self isolation, deletion and camp checks, signed HP delta and caster association. Four additional cases enter cached `BattleStateServer.Trigger`, construct target-generation and skill-phase requests in order, and request `StateTriggerEnd`, with hidden and mismatched owners rejected. Those four cases also execute the actual target/phase bodies and enter current `BattleCmdServer.TriggerCmd`. A new four-case extension replaces the empty list with the exact eight resource-150 rows of phase command 60406 and traverses them through current `GenerateEffectList`; conditions and effect bodies remain outside the probe. Fourteen selected `BattleRecord` cases also match: ten frame constructors for card use, hits, property changes, target selection and state changes, plus four queue lifecycles through battle-cut dispatch. Seven `RunEffectOrder` scenarios match across fresh/resumed orders, post-action refresh, PvE/PvP statistics, cleanup, battle/wave completion and forced-bout routing. Six `lg_UseCard` scenarios match for missing/denied/successful cards, server-target clearing, client-target preservation/replacement and the per-turn play limit. A wider card-path hash check finds BattleUnitBase, BattleCardMgrServer, BattleCardServer, BEBeforeUseCard and BEAfterUseCard unchanged; BattleUnitPlayer changed. BECreateCard is also unchanged. With current BattleConst and BattleLogicEvent, the generated-card path matches 21 inherited cases across request construction, Draw/Hand mutation, full-hand NoneDeck diversion, extra-bout Dimension rejection, owner resolution and command-constructor requests. The authored command-plan layer now joins that boundary to the separately tested current GetSkillArgs normalization and exposes main-command Arg bindings from caller-supplied raw base arguments plus copied card overrides. Its direct and connected energy-payment paths nevertheless match all 222 inherited cases, including X-cost and ignore/force-mode branches through property mutation. The byte-identical `BattleUnitBase.CanUseCard` also matches all 90 inherited ordinary non-keeper PvE eligibility cases when executed with resource-150 dependencies. Eight ordinary `UseCard`/effect-chain construction cases match for missing and hard-blocked cards, target generation or explicit targets, optional pre-commands, and the `RunEffectOrder` wrapper. Six connected before-use cases then match through history insertion, actual energy mutation, card-use recording and optional BeforeUseCard event creation for ordinary, suppressed, attached, forced-free and X-cost branches. Eight after-use cases match explicit/Awake/consumed/attached/graveyard routing, used-card statistics, normal and attached event requests, and end-of-action cleanup. Active effect initialization does contain a confirmed change. In resource 144, repeat modifiers are applied to the supplied repeat and the result is then clamped to at least one. Resource 150 first clamps the base repeat to at least one, then applies the flat and percentage repeat modifiers, and finally clamps again. This changes 12 of the 113 inherited initialization probes, all with supplied repeat -1 or 0; 8 live per-target binding probes and 16 repetition-routing probes remain equal. The routing probes cover owner termination, per-repeat delay, dead single-target regeneration and child-effect creation order. `engine/active-damage-command.mjs` now preserves both initialization orders and exposes the tested routing transition.

This establishes cross-build equality for the utility formula, inherited ordinary PvE offensive assembly, no-card and Strike/Ulti card final target/critical domains, selected property paths, Active repetition routing, the narrow command-entry gate and connected event delivery, while recording the Active repeat-order change explicitly. The installed resource-150 `FuncTable` additionally reproduces all 450 phase-cap condition/parameter sequences for its exact current 60406/60405 command rows. Its remove-state, monster-skill-change and state-manager modules are byte-identical to the baseline modules passing 48 transition-effect fixtures. The separate `MonsterBehaviorComp` changed, so its actual current chunk was executed against 20 inherited `ChangeSkill`/`SetIntention` cases and matched all of them. Change type `1` inserts the previous idle intent into the temporary queue, installs the transition intent and refreshes its preview without executing it. Sixty more cases execute add-state routing through the live manager into existing-counter `AddLayer`, then subtract-state routing through the same registry into phase `SubLayer`; the changed resource-150 `BEAddStateParent` matches every inherited result, while the other four participating modules are byte-identical. Sixteen absent-state cases now execute original construction and registration for phase-related states, five counter cases continue through actual `InitProperty` into `be_damage_statics` storage, and seven second-phase cases store both the transition layer count and resolved 33%-max-HP `be_damage_limit`. Five joined cases then use current command/effect construction, byte-identical `TryDoEffect`, and root/subeffect recursion across a narrow callback bridge to the original add/subtract/remove/skill bodies over one registry, matching the authored first- and second-phase transitions. The changed resource-150 parser, expressions, add parent and property server match every inherited case. BattleEffectServer, BattleEffectMgrServer, BEFunctionEffect, BEAttachPostAction, BESendEvent, BattleLogicEvent, BattleEventMgr, Table, BSTHpChanged, BattleStateTriggerServer and BERoleDeadlyDamage are byte-identical, carrying their existing effect/event/fatal-damage evidence forward to resource 150. `BERoleDie` and `BattleUnitMonster` changed; the new path delegates PvE and first-free-respawn eligibility to role methods. Executing the actual changed `BERoleDie`, `BattleUnitMonster` and `BattleUnitPlayer` chunks with resource-150 constants matches all eight selected missing-role, revived, monster, PvE-player, PvP and monster-eligibility fixtures. The main skill-phase boundary is narrower than the earlier inventory implied: BEGenerateTargets, BECreateSkillPhase, BattleCmdTargetsExp, BattleEffectServer and BattleEffectMgrServer are byte-identical. Within the changed BattleCmdServer, BattleCmdParser, BattlePropertyServer and BEActiveDamage path, 257 inherited cases now match exactly across connected phase start through `TriggerCmd`, original command-to-effect-manager construction, real Active and Function-effect construction/target and parameter binding/initialization/repetition routing/function dispatch into `Damage2SingleTarget`, `GetRealDmg`, byte-identical `BattleUnitBase.BeHit`, HP mutation, original nonlethal event-request order and one lethal monster request boundary, phase cleanup, StateOwner target handoff, ArgN lookup, connected skill-argument construction, command-delay planning and effect-row ordering. The lethal case reaches zero HP, requests `BERoleDeadlyDamage` then `BERoleDie`, and requests `ActiveDamageKill` after `DoDamage` and `BeDamage`. The connected hit cases use explicit neutral PvE adapters and empty skill types; property-send, hit-record and event-factory callbacks are observers. Generic event delivery, the HP listener and cached state request construction are proven separately, but real state command rows and scheduler completion are not yet executed from the hit path; animation and a fully scheduled death tree also remain outside the boundary. A separate live-catalog audit finds 69 Awake skills and no directly linked damage rows in either build; one-level literal traversal reaches one conditional Passive-damage row, and resource 150 adds one direct RemoveState row. Awake nested/state/resource execution, broader property mutation, keeper/PvP/forced-play eligibility outside the selected cases, listener-generated effect trees, full death scheduling, card-manager mutation internals, transport serialization/replay playback and other untested component internals remain uncovered.

A bounded resource-150 replay-prediction path is now available for complete live property snapshots. It uses replay-embedded command and skill rows, a classifier generated from all 6,959 installed State rows, exact current `DamagePer2HasState` and selected `BattleApi.Data` mappings, and the cross-build-matched offense, utility, critical and final-target domains. Schema 2 continues through the 18-case matched current `BeHit`/HP path, returning pre-hit damage separately from modeled HP loss after immunity, shield/Puncture, retained-HP, incoming limits and death resistance. All 6,841 State IDs shared with resource 144 keep the same buff/debuff classification; 118 current-only IDs are classified from the installed table. The adapter rejects raw pre-constructor maps, Awake/state-trigger paths and unsupported command routing. Direct retrospective recalculation of the three exact resource-150-catalog replays recovers thirteen critical/noncritical branch matches with zero mismatches. This can support a fresh current-build holdout inside that narrow scope; it does not port the general calculator or make those historical replays build-confirmed.

For a future current-build holdout, capture the resource-150 manifest and script-bundle hashes before the battle, freeze the prediction before outcome inspection, and bind the replay to the same live session. Historical replay timestamps or embedded data rows alone do not establish its engine code.
